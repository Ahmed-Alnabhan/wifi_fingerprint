<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Light Style -->
    <style name="page_background_light">
        <item name="android:background">@drawable/light_background_gradient</item>
    </style>

    <style name="text_large_light">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/size_16_sp</item>
    </style>

    <style name="text_regular_light">
        <item name="android:textColor">@color/bold_gray</item>
        <item name="android:textSize">@dimen/size_14_sp</item>
    </style>

    <style name="text_small_light">
        <item name="android:textColor">@color/bold_gray</item>
        <item name="android:textSize">@dimen/size_12_sp</item>
        <item name="android:background">@color/wifi_light_gray</item>
    </style>

    <!-- Dark Style -->
    <style name="page_background_dark">
        <item name="android:background">@drawable/dark_background_gradient</item>
    </style>

    <style name="text_large_dark">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/size_16_sp</item>
    </style>

    <style name="text_regular_dark">
        <item name="android:textColor">@color/wifi_light_gray</item>
        <item name="android:textSize">@dimen/size_14_sp</item>
    </style>
    <style name="text_small_dark">
        <item name="android:textColor">@color/wifi_light_gray</item>
        <item name="android:textSize">@dimen/size_12_sp</item>
        <item name="android:background">@android:color/background_dark</item>
    </style>
</resources>
